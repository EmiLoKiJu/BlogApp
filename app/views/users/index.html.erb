<!DOCTYPE html>
<html>
<head>
  <title>List of Users</title>
</head>
<body>
  <% if user_signed_in? %>
    <div class='dflex userlogs spacebetween'>
      <div>Welcome, <%= current_user.name %>!</div>
      <%= link_to 'Logout', custom_destroy_user_session_path, class: 'log' %>
    </div>
  <% else %>
    <div class='dflex userlogs spacebetween'><%= link_to 'Login', new_user_session_path, class: 'log' %></div>
  <% end %>
  <ul>
    <% @users.each do |user| %>
      <li class='dflex user'>
        <%= link_to user_path(user) do %>
          <div class='dflex small-img'>
            <img src="<%= user.photo %>" alt="Profile photo">
          </div>
        <% end %>
        <div class='dflex flexcol user-name-nposts'>
          <a href="<%= user_path(user) %>">
            <div class='username'><%= user.name %></div>
          </a>
          <div class='posts-counter'>posts counter: <%= user.posts_counter %></div>
        </div>
      </li>
    <% end %>
  </ul>
</body>
</html>
